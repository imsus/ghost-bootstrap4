<ul class="navbar-nav mr-auto">
  {{#foreach navigation}}
    <li class="nav-{{slug}}{{#if current}} active{{/if}}">
      <a class="nav-link" href="{{url absolute="true"}}">{{label}}{{#if current}} <span class="sr-only">(current)</span>{{/if}}</a>
    </li>
  {{/foreach}}
</ul>